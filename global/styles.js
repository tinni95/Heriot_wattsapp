import Constants from "expo-constants";

import allStyles from "../client-configs/allStyles";

const appSlug = Constants.manifest.slug;
const appStyles = allStyles[appSlug];
export default appStyles;
